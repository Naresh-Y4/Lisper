# Lisper - Share Secrets in Style

**Lisper** is a simple, fun, and interactive web application built using **Node.js**, **Express**, **EJS**, and **Axios**. It fetches and displays random secrets from a public API, showing both the secret and the username who shared it.

---

## 🚀 Features

- Fetches **random secrets** from the [Secrets API](https://secrets-api.appbrewery.com/).
- Displays the secret in a **visually appealing card** with CSS styling.
- Shows the username associated with each secret.
- **Responsive design** that works on desktop and mobile.
- **Error handling**: displays a friendly message if the API fails.

---

## 🛠 Technologies Used

- **Backend:** Node.js, Express.js
- **Frontend:** EJS templates, HTML, CSS
- **HTTP Client:** Axios
- **Version Control:** Git & GitHub

---
